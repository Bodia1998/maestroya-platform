# Hooks

Client-side React hooks (TanStack Query hooks, form hooks, UI-state
hooks). Anything here implies the consuming component is a Client
Component — keep usage as low in the tree as possible per the
Server-Components-by-default rule in docs/ARCHITECTURE.md.

Empty on purpose.
