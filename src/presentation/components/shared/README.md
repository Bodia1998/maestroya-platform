# Shared Components

Composite, app-aware components built from `ui/` primitives (e.g. a
`PageHeader`, `EmptyState`, `Navbar`) that are reused across multiple
routes but aren't generic enough to belong in `ui/`.

Empty on purpose — no routes with real content exist yet.
