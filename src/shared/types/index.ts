/**
 * Cross-cutting types shared across layers (e.g. a generic `Result<T, E>`
 * type for use-case return values). Empty on purpose — add as real
 * cross-cutting needs appear; don't let this become a dumping ground for
 * types that actually belong to one module.
 */
export {};
