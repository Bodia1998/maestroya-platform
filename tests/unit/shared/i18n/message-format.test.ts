// Module 29 — Internationalization.
//
// Retired along with `src/shared/i18n/message-format.ts` (see that
// file's header): the ICU renderer it tested is gone, replaced by real
// `next-intl`'s own ICU formatting. This file could not be removed from
// disk in this environment (the sandbox denied the delete-file
// permission), so it is left as this empty, assertion-free test file.
import { describe, it } from "vitest";

describe.skip("retired: src/shared/i18n/message-format.ts no longer exists", () => {
  it("superseded by next-intl", () => {});
});
